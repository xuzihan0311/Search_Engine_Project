To execute the project, please follow the following steps.

1. Import the whole project into eclipse, notice that you may need to import the library to java project.

2. Run the Crawler.java which contains the main method. After that the database will be generated.

3. After that, a spider_result.txt will be generated which contains all the information.